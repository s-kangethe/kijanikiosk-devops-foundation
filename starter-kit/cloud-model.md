Whether the system uses IaaS, PaaS or SaaS?
The system setup uses IaaS as it controls the filesystem, permissions for access control and user level security configurations which are all infrastructure-level responsibilities rather than just deploying and using software as expected in PaaS and SaaS respectively. 
